/** Minimum row count before @tanstack/react-virtual kicks in. */
export const VIRTUALIZE_THRESHOLD = 50

/** Trade count above which equity/monthly transforms run in a Web Worker. */
export const BACKTEST_WORKER_THRESHOLD = 400
